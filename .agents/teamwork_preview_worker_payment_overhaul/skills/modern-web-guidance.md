# Modern Web Guidance

A skill to search for specific web development use cases and retrieve their corresponding best practice guides.

Trigger immediately for HTML/CSS and client-side JS tasks.
Must search first, use `npx -y modern-web-guidance@latest search "<query>"` if available and network is active. In CODE_ONLY network mode, we can read the local skill path or look up local patterns.
